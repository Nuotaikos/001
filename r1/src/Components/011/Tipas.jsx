// function Tipas({ seaPlaners }) {
//   seaPlaners.sort((a, b) => {

//     if (a.type < b.type) {
//       return -1;
//     }
//     if (a.type > b.type) {
//       return 1;
//     }
//     return 0;
//   });

//   return seaPlaners.map((b, i) => <Tipas style={{ color: b.color }} key={i}>{b.id} {b.type}</Tipas>: null)
// }

// export default Tipas;